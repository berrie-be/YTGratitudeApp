apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gratitudeapp.fullname" . }}-test-connection"
  labels:
    {{- include "gratitudeapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gratitudeapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
